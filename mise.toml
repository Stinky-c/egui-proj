[tools]
rust = {
    version = "1.94",
    targets = "x86_64-pc-windows-msvc,x86_64-unknown-linux-gnu,wasm32-unknown-unknown"
}
"cargo:trunk" = {version = "0.21.14"}

[tasks]
"web:serve" = "trunk serve --open"
"web:build" = "trunk build"
"web:release" = "trunk build --release"
